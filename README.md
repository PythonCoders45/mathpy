# MathPy

A lightweight Python library combining array operations, dataframes, math functions, and utility toolkits into a single package.

---

## 📦 Installation

Once published on PyPI, you can install the package using `pip`:

```bash
pip install mathpy
