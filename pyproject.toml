[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mathpy"  # Must be unique on PyPI!
version = "0.1.0"
description = "Python package for Math and Data!"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
